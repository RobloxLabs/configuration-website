@model IEnumerable<string>

<script type="text/javascript">
    var groupNames = [
        @foreach (var configGroupName in Model)
        {
            @:"@configGroupName",
        }
    ];
</script>